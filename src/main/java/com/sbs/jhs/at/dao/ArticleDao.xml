<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbs.jhs.at.dao.ArticleDao">
	<select id="getForPrintArticles" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus = 0
		ORDER BY id DESC
		LIMIT #{limitFrom}, #{itemsInAPage}
	</select>
	<select id="getForPrintArticle" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus = 0
		AND id = #{id}
	</select>
	<select id="getForPrintListArticlesCount" resultType="_int">
		SELECT COUNT(*)
		FROM article
		WHERE displayStatus = 1
		AND delStatus = 0
	</select>
	<insert id="write">
		INSERT INTO article
		SET regDate = NOW(), 
		updateDate = NOW(), 
		title = #{title}, 
		`body`= #{body},
		displayStatus = 1,
		delDate = NOW()
	</insert>
	<update id="delete">
		UPDATE article
		SET delStatus = 1
		, delDate = NOW()
		, displayStatus = 1
		WHERE id = #{id}
	</update>
	<update id="modify">
		UPDATE article
		SET title = #{title}
		, `body` = #{body}
		, updateDate = NOW()
		WHERE id = #{id}
	</update>
	<select id="getForPrintSearchArticle" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus = 0
		AND title LIKE CONCAT('%', #{searchKeyword} , '%')
		ORDER BY id DESC
		LIMIT #{limitFrom}, #{itemsInAPage}
	</select>
	<select id="getForPrintListSearchArticlesCount" resultType="_int">
		SELECT COUNT(*)
		FROM article
		WHERE displayStatus = 1
		AND delStatus = 0
		AND title LIKE CONCAT('%', #{searchKeyword} , '%')
	</select>
	<insert id="writeReply">
		INSERT INTO articleReply
		SET regDate = NOW(), 
		updateDate = NOW(), 
		`body`= #{body},
		articleId = #{articleId},
		displayStatus = 1,
		delDate = NOW()
	</insert>
	<select id="getForPrintListArticleRepliesCount" resultType="_int">
		SELECT COUNT(*)
		FROM articleReply
		WHERE displayStatus = 1
		AND delStatus = 0
		AND articleId = #{id}
	</select>
	<select id="getForPrintArticleReply" resultType="ArticleReply">
		SELECT *
		FROM articleReply
		WHERE displayStatus = 1
		AND delStatus = 0
		AND articleId = #{id}
		ORDER BY id DESC
		LIMIT #{limitFrom}, #{itemsInAPage}
	</select>
	<update id="replyDelete">
		UPDATE articleReply
		SET delStatus = 1
		, delDate = NOW()
		, displayStatus = 1
		WHERE id = #{articleReplyId}
		AND articleId = #{articleId}
	</update>
</mapper>